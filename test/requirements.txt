sudo apt install can-utils

pip install python-can

pip install cantools

sudo modprobe vcan
sudo ip link add dev vcan0 type vcan
sudo ip link set up vcan0


https://github.com/xR3b0rn/dbcppp/tree/master